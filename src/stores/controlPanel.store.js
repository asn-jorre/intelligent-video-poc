import { writable, readable } from 'svelte/store';

export let logoVisible = writable(true); // done
export let modelCopyVisible = writable(true); // done
export let mainCopyVisible = writable(true);
export let promoBolVisible = writable(true);
